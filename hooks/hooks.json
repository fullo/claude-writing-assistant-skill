{
  "description": "Markua/LeanPub formatting validation hook for manuscript files. Skips non-book projects via shell pre-filter.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/markua-guard.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
